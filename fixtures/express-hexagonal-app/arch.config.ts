/**
 * Legacy config filename (see `CONFIG_FILE_NAMES`); mirrors the main config.
 * Used by `layerlock --discover` smoke tests alongside `layerlock.config.ts`.
 */
import config from "./layerlock.config.js";

export default config;
